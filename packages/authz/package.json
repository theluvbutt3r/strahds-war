{
  "name": "@sw/authz",
  "version": "0.0.0",
  "private": true,
  "license": "UNLICENSED",
  "description": "Permission policy — pure functions, no I/O, exhaustively testable",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist .turbo coverage *.tsbuildinfo"
  },
  "dependencies": {
    "@sw/schemas": "workspace:*"
  },
  "devDependencies": {
    "@sw/tsconfig": "workspace:*",
    "@sw/vitest-config": "workspace:*",
    "@types/node": "22.20.1",
    "typescript": "5.9.3",
    "vitest": "4.1.10"
  }
}
