{
  "name": "@sw/db",
  "version": "0.0.0",
  "private": true,
  "license": "UNLICENSED",
  "description": "Drizzle schema, migrations and query builders — imported ONLY by services/api",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio",
    "db:seed": "tsx src/seed-cli.ts",
    "clean": "rm -rf dist .turbo coverage *.tsbuildinfo"
  },
  "dependencies": {
    "@sw/schemas": "workspace:*",
    "drizzle-orm": "0.45.2",
    "postgres": "3.4.9"
  },
  "devDependencies": {
    "@sw/tsconfig": "workspace:*",
    "@sw/vitest-config": "workspace:*",
    "@types/node": "22.20.1",
    "drizzle-kit": "0.31.10",
    "tsx": "4.23.8",
    "typescript": "5.9.3",
    "vitest": "4.1.10"
  }
}
