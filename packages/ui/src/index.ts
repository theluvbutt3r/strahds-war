/**
 * @sw/ui — shadcn/ui primitives vendored and restyled Barovian.
 *
 * Components are copied in rather than installed, so heavy restyling does not fight the
 * library, while Radix's keyboard and screen-reader behaviour comes along for free.
 *
 * Phase 2 fills this in (docs/PLAN.md §9).
 */

export const UI_PHASE = 2 as const;
